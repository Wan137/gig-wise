# Web framework
fastapi==0.139.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32
sse-starlette==3.4.5

# Database / migrations
sqlalchemy==2.0.51
alembic==1.18.5
psycopg2-binary==2.9.12  # Postgres driver, used in production (Supabase); local dev stays on SQLite

# Auth
pyjwt==2.13.0
bcrypt==5.0.0
email-validator==2.3.0

# Agent orchestration / LLM
langgraph==1.2.8
langchain-core==1.4.9
langchain-groq==1.1.3
groq==0.37.1

# OCR / image preprocessing (requires the Tesseract binary - see README)
pytesseract==0.3.13
pillow==12.3.0
opencv-python-headless==5.0.0.93
numpy==2.5.1

# RAG ingestion / embeddings / vector store
# Embeddings use chromadb's bundled ONNX MiniLM model (app/rag/embeddings.py) -
# no sentence-transformers/torch dependency, which would push runtime memory
# well past free-tier hosting limits (Render's free plan caps at 512MB).
chromadb==1.5.9
langchain-text-splitters==1.1.2
pypdf==6.14.2

# Config / env
python-dotenv==1.2.2
pydantic-settings==2.14.2
